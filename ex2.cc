#include <stdio.h>
#include <ctype.h>

int main() {
    int num;

    printf("Digite o numero desejado: ");
    scanf("%d", &num);

    printf("O numero que voce digitou foi %d", num );

    return 0;
}