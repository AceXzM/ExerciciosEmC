#include <stdio.h>
#include <cmath>

int main () {
    printf("Digite um numero: ");
    int centena;
    int dezena;
    int unidade;
    scanf("%d", &centena, &dezena, &unidade);

    if ( n < 0 || n > 1000 ) {
        printf("O valor digitado esta fora do intervalo de 0 a 1000");
        }
    else if 
}