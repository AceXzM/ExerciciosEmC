#include <stdio.h>

int main () {
    printf("Digite seu primeiro: ");
    int f1;
    scanf("%d", &f1);
    printf("Digite seu segundo: ");
    int f2;
    scanf("%d", &f2);
    printf("A soma dos dois numeros e: %d\n", f1 + f2);
    return 0;

}